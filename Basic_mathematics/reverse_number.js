//Write a program to generate the reverse of a give number N. Print the corresponding reverse numver.
//NOTE: If a Number has Trolling Zeros, Then its Reverse should not include zeros.
//Eg :- N = 10400 reverse = 401 instead of 00401.

let num = 9877
let reverseNum =0;
while(num > 0){
    let lastDigit = num % 10
    reverseNum = (reverseNum * 10);
}